# 性能优化记录

## 问题分析

后端 CPU 占用偏高，主要原因：

### 1. 流式响应中的频繁事件循环切换
- **位置**: `server/runtime/turn_runner.py:95`
- **问题**: 每个 token 都调用 `asyncio.sleep(0)`，高频切换导致 CPU 开销大
- **优化**: 改为每 10 个 chunk 才让步一次

### 2. 同步磁盘 I/O 过于频繁
- **位置**: `session/transcript.py`
- **问题**: 每条 transcript event 都立即写入磁盘，一个 turn 可能触发 20-50 次磁盘 I/O
- **优化**: 添加缓冲区，默认累积 10 条事件后批量写入

### 3. 高频 SQLite 小事务
- **位置**: `session/store.py:append_event`
- **问题**: 每个 event 都触发一次数据库连接和事务提交
- **优化**: 批量缓存索引更新，每 5 次事件或关键事件时才刷新数据库

### 4. 消息历史深拷贝开销
- **位置**: `context_manager/history.py`
- **问题**: 每次构造/替换消息时完整深拷贝，随对话变长线性增加开销
- **优化**: 改为浅拷贝，由调用方保证不修改原始消息

## 优化效果预估

- **流式响应**: CPU 占用降低 ~20-30%
- **磁盘 I/O**: 减少 80-90% 的写入次数
- **数据库事务**: 减少 ~80% 的事务数量
- **内存拷贝**: 长对话场景下性能提升 30-50%

## 注意事项

1. **数据安全性**: 
   - 在 turn 结束时强制刷新缓冲区
   - 进程异常退出时可能丢失最近几条未刷新的事件
   
2. **兼容性**:
   - `TranscriptWriter` 添加了 `flush()` 方法
   - `SessionStore` 需要在合适时机调用 `_flush_index_update()`
   
3. **测试建议**:
   - 验证高并发场景下的数据一致性
   - 测试异常退出时的数据丢失情况
   - 确认长对话场景下的内存使用

## 进一步优化建议

1. **异步 I/O**: 使用 `aiofiles` 替换同步文件操作
2. **连接池**: SQLite 使用连接池减少连接创建开销
3. **JSON 序列化**: 考虑使用 `orjson` 等高性能库替换标准库
4. **消息压缩**: 对历史消息进行增量压缩存储
